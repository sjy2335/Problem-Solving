#include <iostream>
#include <string.h>
#include <vector>

using namespace std;

int main(void) {

	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	unsigned int C;
	cin >> C;

	for (int i = 0; i < C; i++) {
		unsigned int H, W;
		cin >> H, W;
		char** map = new char* [H];
		for (int j = 0; j < H; j++) {
			map[j] = new char[W];
			

			}
		}
	}

	return 0;
}