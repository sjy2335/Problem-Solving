#include <iostream>
#include <vector>

using namespace std;

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int N, n;
    cin >> N;
    vector<int> A;

    for (size_t i = 0; i < N; i++)
    {
        cin >> n;
        A.push_back(n);
    }

    for (size_t i = 0; i < N; i++)
    {
        
    }
    
    

    return 0;
}